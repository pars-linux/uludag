.. _toolchain-freeze:

Toolchain Freeze
================

The toolchain is freezed at some point and the building structure of the release is fixed and can not been changed without release management group approval.

Before toolchain freeze below conditions must be considered:


